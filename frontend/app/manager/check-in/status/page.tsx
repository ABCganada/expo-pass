import { Activity } from "lucide-react";
import { FeaturePlaceholder } from "@/features/shared/components/FeaturePlaceholder";

export default function CheckInStatusPage() {
  return <FeaturePlaceholder icon={Activity} title="체크인 현황" description="전체 예약자 대비 입장 완료 인원을 실시간으로 확인하는 화면입니다." />;
}
